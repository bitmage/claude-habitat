# Security - never commit private keys or secrets
*.pem
shared/github_deploy_key
shared/github_deploy_key.pub
shared/*.private-key.*

node_modules/
*.log
.DS_Store
.last-config
temp-*.yaml
.maintenance-session.md

# Tools (downloaded on demand, not committed)
system/tools/bin/
system/tools/.tmp/
