# Security - never commit private keys or secrets
*.pem
shared/*.private-key.*
# Note: We now use GITHUB_TOKEN environment variable instead of SSH keys

node_modules/
*.log
.DS_Store
.last-config
temp-*.yaml
.maintenance-session.md

# Tools (downloaded on demand, not committed)
system/tools/bin/
system/tools/.tmp/
.last-used-config

# UI testing snapshots (generated, not committed)
test/ui-snapshots.txt
